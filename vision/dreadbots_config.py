sensor="gear"
imagedir="/home/pi/images/"
tmpimagedir="/var/tmp/images/"

"""
Unstable on boot up.
ipaddress=None
if not(ipaddress):
    # TODO: change to the IP address and port on the router.
    import socket
    ipaddress = [(s.connect(('8.8.8.8', 53)), s.getsockname()[0], s.close()) for s in [socket.socket(socket.AF_IN
"""
